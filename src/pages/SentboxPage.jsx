import Sentbox from "../components/Sentbox";
import Layout from "../Layout/Layout";

const SentboxPage = () => {
  return (
    <Layout>
      <Sentbox />
    </Layout>
  );
};

export default SentboxPage;
